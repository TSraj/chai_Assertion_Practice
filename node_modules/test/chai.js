const chai = require('chai');
//expect

const expect = chai.expect;

//expect API example

let a=1, b=2;
expect(a).to.be.equals(b, "a and b are not eual");

//object/string/boolean

function myObj(){
    return{
        a: 100,
        b:'Hello'
    }
}

x = new myObj(), y = new myObj();
expect.to.be.an('object');